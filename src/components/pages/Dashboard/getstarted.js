import React from "react";

export class GetStarted extends React.Component {
  render() {
    return (
      <div className="get-started">
        To get started, click the + add pet link above and create your first pet
        profile.
      </div>
    );
  }
}

export default GetStarted;
